[variables]
NIXPACKS_NO_BUILD_ENV = "1"

[phases.setup]
nixPkgs = ["gcc", "gettext", "nodejs"]

[start]
cmd = "python manage.py collectstatic --noinput && daphne -b 0.0.0.0 -p $PORT ecommerce.asgi:application"

import 'package:flutter/material.dart';

class [ClassName] extends StatelessWidget {
  const [ClassName]({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('[ClassName]')));
  }
}